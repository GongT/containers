#!/usr/bin/bash

if [[ -z ${CONFIG_FILE_LOCK-} ]]; then
	source /etc/environment
fi

if [[ $# -eq 0 ]]; then
	set -- sleep infinity
fi

# touch "${CONFIG_FILE_LOCK}"
# echo "write lock ${CONFIG_FILE_LOCK}"
exec flock "${CONFIG_FILE_LOCK}" "$@"
